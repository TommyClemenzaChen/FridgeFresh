apiVersion: v1
kind: Pod
metadata:
  name: tomy
spec:
  containers:
  - name: mypod
    image: gitlab-registry.nrp-nautilus.io/prp/jupyter-stack/prp-pytorch2:9e8eb7c4
    resources:
      limits:
        memory: 100Mi
        cpu: 100m
      requests:
        memory: 100Mi
        cpu: 100m
    command: ["sh", "-c", "echo 'Im a new pod' && sleep infinity"]